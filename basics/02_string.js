// strings

let userName="Mr John";

console.log(userName);
// string values with repeat attr default() not repeat
//  and we can assign number of repeatation value
// like: 1,2,3.....etc.
console.log(userName.repeat());
// string with concat prop
console.log(userName.concat(" ",", today we faced earthquake earlier."));
// string with slice
console.log(userName.slice(2));
// another way to create string

let message=new String("Hello Mrs. Mariam");
// this method store as string value as array 
console.log(message);
// if we need to print string value only then we need to use toString prop
console.log(message.toString());

